# Local Logs

This directory is reserved for local debug logs produced by benchmark runs and Kaggle replay analysis.

The log files are ignored by Git because they are machine-specific and can become large. Keep important conclusions in the project README or in `experiments/` notes instead.
