# V032-R1

R1 replaces the old two-event heuristic with a shared official-price lockstep rollout, two future same-item events, simultaneous margin estimation, and optional residual calibration.

An empty calibration payload intentionally makes the timing candidate fall back to order-only. Generate calibration from local notebook opponents before treating timing results as a candidate.
