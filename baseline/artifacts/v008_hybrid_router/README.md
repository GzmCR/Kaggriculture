# V008 Hybrid Router Artifacts

Generated threshold sweeps and six-seed benchmark results for the hybrid
current/frontier candidate live here. Raw results include route, distance,
fallback, action, runtime, and final-cash statistics.

The raw rows also contain daily cash observations, route history, fallback
counts, field operation counts, market operation counts, and final statuses.
The selected tuning candidate did not pass the holdout minimum-cash gate, so
the root submission was intentionally left unchanged.
